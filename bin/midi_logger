#!/usr/bin/env ruby

require 'bundler/setup'
require 'unimidi'

input = UniMIDI::Input.all[1]
input.open do |input|
  $stdout.puts "send some MIDI to your input now..."

  loop do
    m = input.gets
    $stdout.puts(m)
  end
end